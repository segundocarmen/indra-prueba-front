import './style.scss';

const Loader = () => {
    return (
        <div className='loading-component'>
            <div className='container'>Cargando...</div>
        </div>
    );
};

export default Loader;
